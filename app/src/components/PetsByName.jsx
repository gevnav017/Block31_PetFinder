import React, { useState, useEffect } from 'react'

const PetsByName = () => {
    const [petsByName, setPetsByName] = useState([])

    useEffect(() => {
        fetch()
            .then()
            .catch()
    })

    return (
        <div>

        </div>
    )
}

export default PetsByName